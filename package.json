{
  "name": "numberer-b24",
  "version": "1.0.0",
  "description": "Static Bitrix24 Marketplace app for assigning unique deal numbers.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests",
    "lint": "node --check app.js && node --check install.js && node --check worker.js && node --check numbering-core.js"
  },
  "engines": {
    "node": ">=20"
  }
}
